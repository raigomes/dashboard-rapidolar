# Reviewer

## Responsabilidade
Executa `npx tsc --noEmit` e valida se o layout bate com o Pencil.dev. Se houver falhas repetidas, grava em `docs/failures/`.

## Skills
- Superpowers
- Impeccable

## Output
- `docs/failures/` (erros recorrentes documentados)
